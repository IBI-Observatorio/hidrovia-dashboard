[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# Volume configurado no dashboard Railway:
# Nome: sema-cache
# Mount path: /data
# Em Variables: DATA_DIR=/data
